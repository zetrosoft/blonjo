import React from 'react';
import InventoryControl from './InventoryControl';

/**
 * StockLevel Sub-Menu Alias — Terintegrasi Penuh ke Single Canvas Inventory Control
 * Seluruh fungsi visual level bar, alert stok, dan kontrol inventory telah dipadukan.
 */
export default function StockLevel() {
  return <InventoryControl />;
}
